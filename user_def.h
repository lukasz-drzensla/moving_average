#define OWN_TYPE float //here we can define what variable type will be used to calculate the average - e.g. int or float

/*here we can define how many components at maximum we want to take to calculate the average,
if we have more the first will be discarded. Usually a fairly big number - like 100, for the purpose of the
example it was set to 3
*/
#define MAX_SIZE 3 
